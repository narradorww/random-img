import React from 'react';
import ReactDOM from 'react-dom/client';

import RandomImageGenerator from './RandomImageGenerato';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RandomImageGenerator />
  </React.StrictMode>
);

